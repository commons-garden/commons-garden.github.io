{
  "scripts": {
    "dev": "rm -rf public && hugo server",
    "pretty": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-go-template": "^0.0.15"
  }
}
